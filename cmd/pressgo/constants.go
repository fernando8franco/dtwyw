package main

const (
	credentialsCmd = "credentials"
	compressCmd    = "compress"

	initHelpFlag = "help"
	noInitFlag   = "no-init"
	initFlag     = "init"

	toolCompress = "compress"
	region       = "us"

	configFile    = "pressgo.config.json"
	pdfExt        = ".pdf"
	titleFilename = "base"
)
